services:
  jenkins:
    image: nexus3:8082/jenkins
    container_name: jenkins
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/.docker/config.json:/etc/docker/daemon.json
      - trivy_cache:/root/.cache
    ports:
      - "8080:8080"
      - "50000:50000"
    networks:
      - dev
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M

networks:
  dev:

volumes:
  jenkins_home:
  trivy_cache:
